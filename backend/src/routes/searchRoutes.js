/*
const express = require('express');
const router = express.Router();
const searchController = require('../controllers/searchController');

// Main search endpoint - supports all your criteria
router.get('/', searchController.search);

// Optional: Add these specialized endpoints later if needed
// router.get('/students', searchController.searchStudents);
// router.get('/teachers', searchController.searchTeachers);
// router.get('/parents', searchController.searchParents);

module.exports = router;*/